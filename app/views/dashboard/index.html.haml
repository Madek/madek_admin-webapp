-# %h2= @data[:environment][:madek][:site_title]

%section{style: 'margin: 3rem 0 5rem'}
  %h2.h3 Resources
  = render partial: 'resource_statistics'

- if current_user.has_admin_permission?('sql_assistant')
  %section{style: 'margin-bottom: 5rem'}
    %h2.h3 Assistant
    = link_to 'Go to the section &rarr;'.html_safe, assistant_path

%section
  %h2.h3 System
  = render partial: 'system_info'
