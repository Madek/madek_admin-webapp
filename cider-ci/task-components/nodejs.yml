traits:
  asdf: true
scripts:
  nodejs-asdf-install:
    exclusive_executor_resource: asdf-nodejs
    body: ./cider-ci/bin/nodejs-setup
  test:
    start_when:
      nodejs is installed:
        script_key: nodejs-asdf-install
