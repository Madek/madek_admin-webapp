# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   'true': 'foo'
#
# To learn more, please read the Rails Internationalization guide
# available at https://guides.rubyonrails.org/i18n.html.

en:
  activerecord:
    attributes:
      context:
        id: ID
      context_key:
        documentation_url: Documentation URL
        id: ID
        meta_key_id: MetaKey
      group:
        id: ID
        name: Name
      meta_key:
        allowed_people_subtypes: Allowed Subtypes
        allowed_rdf_class: Allowed Type
        id: ID
        documentation_url: Documentation URL
      keyword:
        id: ID
        meta_key_id: MetaKey
        term: Term
      user:
        email: E-mail
        id: ID
        login: Login
      vocabulary:
        id: ID
  second_step_flash:
    allowed_people_subtypes: Select at least one Allowed Subtype!
    allowed_rdf_class: 'Select one Allowed Type.' 
    is_extensible_list: Decide if keywords should be an extensible list.
    keywords_alphabetical_order: Choose if keywords should be alphabetically ordered.
    text_type: Choose text type.
  session_expiring_soon: You will be logged out in less than %{minutes} minutes. Save your input and log in again.
